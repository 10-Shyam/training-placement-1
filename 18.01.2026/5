num = int(input("Enter number: "))
sum = 0
temp = num

while temp > 0:
    d = temp % 10
    sum = sum + d*d*d
    temp = temp // 10

if num == sum:
    print("Armstrong Number")
else:
    print("Not Armstrong")
